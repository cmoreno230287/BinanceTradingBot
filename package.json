{
  "name": "binance-trading-bot-v2",
  "version": "1.0.0",
  "description": "TypeScript trading bot for BTCUSDT analysis and Binance order execution via Binance Integration CLI.",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:real": "node dist/index.js --real",
    "start:test": "node dist/index.js --test",
    "analyze": "npm run build && npm run start",
    "backtest": "npm run build && node dist/backtest.js",
    "watch": "npm run build && node dist/index.js",
    "publish": "npm run build && powershell -ExecutionPolicy Bypass -File publish.ps1"
  },
  "keywords": [
    "binance",
    "trading-bot",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
